// Hardcoded for this iteration. Token management may graduate to a Supabase
// table (server-side validation) in a future iteration; see
// changelog-access-token-gate.md.
export const VALID_ACCESS_TOKEN = "EQ895";
